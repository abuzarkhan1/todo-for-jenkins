pipeline {
    agent any

    tools { nodejs "node" }
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/abuzarkhan1/todo-for-jenkins'
                sh 'npm install'
            }
        }
    }
}
